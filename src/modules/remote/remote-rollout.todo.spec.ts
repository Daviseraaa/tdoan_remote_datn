/**
 * Checklist rollout / kiểm thử remote (thực hiện tay + staging).
 */
describe('Remote WebRTC rollout checklist', () => {
  test.todo('LAN: tạo phiên, video, chuột/scroll/text');
  test.todo('NAT di động: cấu hình TURN (WEBRTC_TURN_*)');
  test.todo('RBAC USER vs ADMIN; session lock; heartbeat timeout; panic audit');
});
